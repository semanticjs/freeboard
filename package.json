{
  "name": "@semanticjs/freeboard",
  "title": "@semanticjs/freeboard",
  "version": "0.0.5",
  "scripts": {
    "build": "grunt",
    "deploy": "npm version patch && npm run deploy:all",
    "deploy:all": "npm version patch && npm run build && npm publish ./ --access public",
    "start": "http-server"
  },
  "devDependencies": {
    "grunt": "0.4.1",
    "grunt-contrib-concat": "0.1.3",
    "grunt-contrib-cssmin": "0.6.1",
    "grunt-contrib-uglify": "0.6.0",
    "grunt-contrib-watch": "0.5.3",
    "grunt-string-replace": "^0.2.7",
    "http-server": "^0.12.0"
  }
}
